import { BrowserRouter } from "react-router-dom";
import { RouterApp } from "./Router/RouterApp";

const App = () => {
  return (
    <BrowserRouter>
      <RouterApp />
    </BrowserRouter>
  );
};

export default App;
