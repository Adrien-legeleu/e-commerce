export const FunctionFilter = () => {
  const filterColor = (valueToFilter, products) => {
    return products.filter((product) => valueToFilter.includes(product.color));
  };
  const FunctionFilter = (valueToFilter, products) => {
    return products.filter((product) => valueToFilter.includes(product.color));
  };
  const FunctionFilter = (valueToFilter, products) => {
    return products.filter((product) => valueToFilter.includes(product.color));
  };
};
